import type { CSSProperties } from "react";

/** Co-located styles for one agent's eval history page. */
export const s = {
  back: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    fontSize: 13.5,
    color: "var(--text-secondary)",
    textDecoration: "none",
    marginBottom: 16,
  } satisfies CSSProperties,
  header: {
    display: "flex",
    alignItems: "flex-start",
    gap: 20,
    marginBottom: 22,
  } satisfies CSSProperties,
  titleRow: {
    display: "flex",
    alignItems: "center",
    gap: 12,
    flexWrap: "wrap",
  } satisfies CSSProperties,
  h1: { fontSize: 26, fontWeight: 700, margin: 0 } satisfies CSSProperties,
  subtitle: { fontSize: 14, color: "var(--text-secondary)", marginTop: 6 } satisfies CSSProperties,
  headActions: {
    marginLeft: "auto",
    display: "flex",
    alignItems: "center",
    gap: 8,
    flexShrink: 0,
  } satisfies CSSProperties,
  rangeWrap: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "6px 10px",
    borderRadius: 7,
    border: "1px solid var(--border-strong)",
    background: "var(--bg-elevated)",
  } satisfies CSSProperties,
  rangeSelect: {
    fontSize: 13,
    color: "var(--text-primary)",
    background: "transparent",
    border: "none",
    outline: "none",
    cursor: "pointer",
  } satisfies CSSProperties,
  banner: {
    display: "flex",
    alignItems: "flex-start",
    gap: 12,
    padding: "14px 18px",
    borderRadius: 9,
    border: "1px solid var(--warn)",
    background: "var(--warn-bg, var(--bg-elevated))",
    color: "var(--text-primary)",
    fontSize: 14,
    lineHeight: 1.5,
    marginBottom: 20,
  } satisfies CSSProperties,
  metricRow: { display: "flex", gap: 14, marginBottom: 22 } satisfies CSSProperties,
  trendPanel: {
    borderRadius: 10,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    padding: "18px 20px 12px",
    marginBottom: 28,
  } satisfies CSSProperties,
  trendHead: {
    display: "flex",
    alignItems: "center",
    gap: 12,
    marginBottom: 10,
    flexWrap: "wrap",
  } satisfies CSSProperties,
  legend: {
    marginLeft: "auto",
    display: "flex",
    gap: 16,
    fontSize: 12.5,
    color: "var(--text-secondary)",
  } satisfies CSSProperties,
  legendItem: { display: "inline-flex", alignItems: "center", gap: 6 } satisfies CSSProperties,
  legendSwatch: (color: string): CSSProperties => ({
    width: 12,
    height: 2,
    borderRadius: 2,
    background: color,
  }),
  runsHead: {
    display: "flex",
    alignItems: "center",
    gap: 12,
    marginBottom: 12,
    flexWrap: "wrap",
  } satisfies CSSProperties,
  selectedNote: { fontSize: 12.5, color: "var(--text-muted)" } satisfies CSSProperties,
  table: {
    borderRadius: 10,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    overflow: "hidden",
  } satisfies CSSProperties,
  grid: {
    display: "grid",
    gridTemplateColumns: "34px 1.2fr 70px 1.1fr 1.1fr 1.1fr 70px 70px",
    alignItems: "center",
    gap: 14,
    padding: "12px 18px",
  } satisfies CSSProperties,
  headRow: {
    background: "var(--bg-surface)",
    borderBottom: "1px solid var(--border)",
  } satisfies CSSProperties,
  th: {
    fontSize: 10.5,
    fontWeight: 700,
    letterSpacing: "0.08em",
    textTransform: "uppercase",
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  bodyRow: { borderTop: "1px solid var(--border)" } satisfies CSSProperties,
  cellMono: { fontSize: 12.5, color: "var(--text-secondary)" } satisfies CSSProperties,
  cellVersion: { fontSize: 12.5, color: "var(--accent)", fontWeight: 600 } satisfies CSSProperties,
  cellPass: { fontSize: 13, fontWeight: 700 } satisfies CSSProperties,
  cellCost: { fontSize: 12.5, color: "var(--text-secondary)" } satisfies CSSProperties,
  empty: {
    fontSize: 14,
    color: "var(--text-secondary)",
    padding: "10px 0 30px",
  } satisfies CSSProperties,
} as const;
