import type { CSSProperties } from "react";

/** Co-located styles for the all-agents Eval Dashboard. */
export const s = {
  header: {
    display: "flex",
    alignItems: "flex-start",
    gap: 20,
    marginBottom: 26,
  } satisfies CSSProperties,
  h1: { fontSize: 26, fontWeight: 700, margin: 0 } satisfies CSSProperties,
  subtitle: {
    fontSize: 14,
    color: "var(--text-secondary)",
    marginTop: 6,
  } satisfies CSSProperties,
  headActions: { marginLeft: "auto", flexShrink: 0 } satisfies CSSProperties,
  skipped: {
    fontSize: 13,
    color: "var(--text-secondary)",
    background: "var(--bg-elevated)",
    border: "1px solid var(--border)",
    borderRadius: 8,
    padding: "10px 14px",
    marginBottom: 20,
  } satisfies CSSProperties,
  cards: {
    display: "flex",
    flexDirection: "column",
    gap: 10,
    marginBottom: 32,
  } satisfies CSSProperties,
  card: {
    display: "flex",
    gap: 18,
    padding: "16px 18px",
    borderRadius: 10,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    textAlign: "left",
    width: "100%",
    cursor: "pointer",
  } satisfies CSSProperties,
  cardIcon: {
    width: 38,
    height: 38,
    borderRadius: 9,
    display: "grid",
    placeItems: "center",
    background: "var(--bg-hover)",
    color: "var(--accent)",
    flexShrink: 0,
  } satisfies CSSProperties,
  cardMain: { flex: 1, minWidth: 0 } satisfies CSSProperties,
  cardTitleRow: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    flexWrap: "wrap",
  } satisfies CSSProperties,
  cardName: { fontSize: 16, fontWeight: 700, color: "var(--text-primary)" } satisfies CSSProperties,
  cardSub: { fontSize: 12.5, color: "var(--text-muted)", marginTop: 5 } satisfies CSSProperties,
  cardRight: {
    display: "flex",
    alignItems: "center",
    gap: 26,
    flexShrink: 0,
  } satisfies CSSProperties,
  stat: { textAlign: "center", minWidth: 62 } satisfies CSSProperties,
  statLabel: {
    fontSize: 10.5,
    fontWeight: 700,
    letterSpacing: "0.08em",
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  statValue: (color: string): CSSProperties => ({
    fontSize: 22,
    fontWeight: 700,
    color,
    marginTop: 2,
  }),
  table: {
    borderRadius: 10,
    border: "1px solid var(--border)",
    background: "var(--bg-elevated)",
    overflow: "hidden",
  } satisfies CSSProperties,
  tr: {
    display: "grid",
    gridTemplateColumns: "1.4fr 1fr 60px 1.1fr 1.1fr 1.1fr 70px",
    alignItems: "center",
    gap: 14,
    padding: "12px 18px",
    borderTop: "1px solid var(--border)",
  } satisfies CSSProperties,
  trFirst: {
    display: "grid",
    gridTemplateColumns: "1.4fr 1fr 60px 1.1fr 1.1fr 1.1fr 70px",
    alignItems: "center",
    gap: 14,
    padding: "12px 18px",
  } satisfies CSSProperties,
  cellAgent: { fontSize: 13.5, fontWeight: 600 } satisfies CSSProperties,
  cellMono: { fontSize: 12.5, color: "var(--text-secondary)" } satisfies CSSProperties,
  cellVersion: { fontSize: 12.5, color: "var(--accent)", fontWeight: 600 } satisfies CSSProperties,
  cellPass: { fontSize: 13, fontWeight: 700, textAlign: "right" } satisfies CSSProperties,
  empty: {
    fontSize: 14,
    color: "var(--text-secondary)",
    lineHeight: 1.6,
    padding: "10px 0 30px",
  } satisfies CSSProperties,
} as const;
