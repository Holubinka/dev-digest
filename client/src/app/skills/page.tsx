import { SkillsView } from "./_components/SkillsView";

/* Route: /skills (Skills Lab). Thin route entry — the two-column shell, its
   list, detail pane, styles and i18n are colocated under _components. */
export default function SkillsPage() {
  return <SkillsView />;
}
